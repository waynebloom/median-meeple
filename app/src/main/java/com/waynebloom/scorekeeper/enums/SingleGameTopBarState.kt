package com.waynebloom.scorekeeper.enums

enum class SingleGameTopBarState {
    Default,
    SearchBarOpen,
    SortMenuOpen
}

enum class GamesTopBarState {
    Default,
    SearchBarOpen
}